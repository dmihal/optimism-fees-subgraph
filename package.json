{
  "name": "optimism-fees",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dmihal/optimism-fees",
    "create-local": "graph create --node http://localhost:8020/ dmihal/optimism-fees",
    "remove-local": "graph remove --node http://localhost:8020/ dmihal/optimism-fees",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dmihal/optimism-fees"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
